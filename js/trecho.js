const doisDestinos = () => {
    var cityOrigin = prompt("Qual a cidade de origem??");
    var cityDestiny = prompt("Qual a cidade de destino??");
}

const custoCalculado = (km) => km * localStorage.getItem('custo');

const escreve = () => {
    alert('Distancia eh de: ${} e total gasto eh de');
}
